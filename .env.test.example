# The test suite's own database. Copy to .env.test.local and fill in the
# password from .env — tests/setup.ts refuses to seed unless the database name
# ends in `_test`, because these specs delete what they create.
DATABASE_URL="postgres://admin:CHANGEME@localhost:5433/foxpassportrepublic_test"
